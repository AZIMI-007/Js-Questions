// 9. Ask the user for the radius of a sphere. Print its volume:
// volume = (4/3) * π * r^3
// (use 3.14 for π)

let radius=Number(prompt("enter radius of a sphere "))
let volume = (4/3) * Math.PI * radius*3*3*3
console.log(volume)